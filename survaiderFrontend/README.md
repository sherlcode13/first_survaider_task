# survaider-frontend

To run 

1. Fork the code in your machine
2. cd survaiderFrontend
3. npm install
4. bower install

## Build & development

Run `grunt` for building and `grunt serve` for preview.

## Testing

Running `grunt test` will run the unit tests with karma.
